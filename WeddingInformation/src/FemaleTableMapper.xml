<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.FemaleTableMapper">

<insert id="insertSurgery" parameterType="FemaleVO">
insert into female_table(
		id
		,surgery
	) values (
		#{id}
		,#{surgery}
	)
</insert>

</mapper>
